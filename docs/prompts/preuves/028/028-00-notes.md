# Preuves 028 — staging (:8001)

## #3 — Message d'erreur backend générique
`028-01-message-erreur-generique.jpg` : bandeau « **Backend non disponible — vérifiez que le serveur API est démarré.** » — plus aucune mention du **port 8000** (faux en staging où l'API est sur :8001). Le bandeau est déclenché par l'événement `api:backend:down` que l'intercepteur axios émet à chaque échec réseau (`error.request` sans réponse) ; c'est le rendu réel du composant `AppShell`.

## #4 — Rafraîchissement « Productions en cours » après renommage
`028-02-refresh-panneau-apres-renommage.jpg` : la production active `prod03 …` a été renommée en `… RENOMME-028` via le menu ⋮ → Renommer. **Sans clic sur « Actualiser »**, le panneau latéral « Productions en cours » affiche immédiatement le **nouveau nom** (toast de confirmation visible en haut). La production a ensuite été **renommée à son nom d'origine** (staging laissé propre).
